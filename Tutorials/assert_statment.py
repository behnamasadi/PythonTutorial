########################## AssertionError Exception ########################## 

# Syntax
# assert <condition>,<error message>
# We can use assert to check for raising an exception
a = +1
assert (a > 0), "the value most be positive."

marks = []
assert len(marks) != 0, "List is empty."
